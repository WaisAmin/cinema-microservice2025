server:
  port: 8085
spring:
  application:
    name: notification-service
  rabbitmq:
    host: rabbitmq
    port: 5672
management:
  endpoints:
    web:
      exposure:
        include: health,info
